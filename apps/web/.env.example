# Fastify API — server-side only (Next.js API routes call Fastify, never client-side)
INTERNAL_API_URL=http://localhost:3001

# PostHog — find in PostHog project settings
NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
