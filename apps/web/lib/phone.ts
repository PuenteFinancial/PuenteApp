// Moved to @puente/shared (src/phone.ts) — mobile normalizes phone numbers for
// the same Supabase phone-auth format.
export { OTP_RESEND_COOLDOWN_SECONDS, SIGNUP_PHONE_KEY, normalizePhone } from '@puente/shared'
