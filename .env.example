# Root .env.example — see apps/api/.env.example and apps/mobile/.env.example for app-specific vars
# Use Doppler to manage secrets across environments: https://doppler.com
# NEVER commit real .env files
