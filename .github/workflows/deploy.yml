name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy-api:
    name: Deploy API to Railway (staging)
    runs-on: ubuntu-latest
    needs: [] # add 'check' job as dependency once CI is green consistently
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Railway
        run: |
          echo "Connect Railway via: railway link && railway up"
          echo "Add RAILWAY_TOKEN to GitHub secrets and use railwayapp/railway-github-action"
        # TODO: add railwayapp/railway-github-action@v2 once Railway project is linked
